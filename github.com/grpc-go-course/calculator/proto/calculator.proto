syntax = "proto3";

package calculator;

option go_package = "github.com/grpc-go-course/calculator/proto";
import "sum.proto";

service CalculatorService {
  rpc Sum(SumRequest) returns (SumResponse);
}